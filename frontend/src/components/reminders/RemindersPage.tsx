// =============================================================================
// Reminders Page
// =============================================================================
// Placeholder for appointment, medication, and wellness reminders.
// Veterans can set reminders for VA appointments, medications,
// and wellness activities.
// =============================================================================

/**
 * RemindersPage — Placeholder component for the reminders feature.
 * Will be expanded with full CRUD functionality in a future milestone.
 *
 * @returns {JSX.Element} The reminders page
 */
export default function RemindersPage() {
  return (
    <div className="page">
      <h2>Reminders</h2>
      <p>
        Set reminders for VA appointments, medications, and wellness activities.
        This feature is coming soon.
      </p>
    </div>
  );
}
